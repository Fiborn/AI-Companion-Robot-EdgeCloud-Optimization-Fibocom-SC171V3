# 边缘计算层依赖（SC171 开发套件 V3 / Ubuntu 20.04，Python 3.8）
# 说明：SC171 开发套件 V3 出厂系统自带 Python 3.8，以下为板端采集与算法所需依赖。

# ---------- 传感器总线 ----------
smbus2>=0.4.2          # I2C：SHT40 / BH1750FVI / SGP30 / SCD41 / APDS-9960
pyserial>=3.5          # UART：PMS5003 / LD2410C / MR60BHA1

# ---------- 视觉与算法 ----------
opencv-python>=4.8.0   # 摄像头取流、JPEG 编码
numpy>=1.24.0          # 数值计算

# ---------- 姿态检测推理 ----------
onnxruntime>=1.16.0    # ONNX 模型推理（人体关键点检测）
# 注：NPU 加速与端侧 LLM 部署走广和通官方 Fibo AI Stack（随开发套件 V3 提供），不走 pip。

# ---------- 与平台通信 ----------
requests>=2.31.0       # 板端 HTTP API（:8765）给 MCP 桥接调用
